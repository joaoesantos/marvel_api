package org.example.marvel.api;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCharactersIT extends CharactersTest {

    // Execute the same tests but in native mode.
}